'use strict'
// 함수선언
function sqArea(width, heigth){
  return width * heigth
}
  console.log(`가로 : 2, 세로 : 3인 사각형의 넓이는 ${sqArea(2,3)}입니다.`)

function triArea(width, heigth){
  return width * heigth / 2
}
  console.log(`가로 : 2, 세로 : 3인 삼각형의 넓이는 ${sqArea(2,3)}입니다.`)
